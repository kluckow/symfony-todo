{% extends 'base.html.twig' %}

{% block maincontent %}
    <h1>Store creation</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('backend_list_stores_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
