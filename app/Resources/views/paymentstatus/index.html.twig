{% extends 'base.html.twig' %}

{% block body %}
    <h1>Paymentstatuses list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for paymentStatus in paymentStatuses %}
            <tr>
                <td><a href="{{ path('paymentstatus_show', { 'id': paymentStatus.id }) }}">{{ paymentStatus.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('paymentstatus_show', { 'id': paymentStatus.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
